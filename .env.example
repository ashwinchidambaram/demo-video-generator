# Copy to .env (gitignored). Do not commit secrets.

# D1: gates Phase 4. If Lyria preview is unreachable on this key,
# document the chosen fallback in DECISIONS.md before starting Phase 4.
GEMINI_API_KEY=

# Optional: future fallbacks per D1
# SUNO_API_KEY=
# STABLE_AUDIO_API_KEY=
