# /eval-agents — run agent eval suites (Phase 1+)

Usage: `/eval-agents [<agent-name>...] [--label headline|smoke|holdout]`

Phase 0 stub. The eval runner skeleton wires up in Phase 1 (deferred from
Phase 0 in v2.1). When wired, this command runs the named eval suite via
`evals/runner.py` and prints rubric + judge scores.
